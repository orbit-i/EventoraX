# Org Admin Guide

Status: PENDING — same reason as the User Guide: depends on the
finished, wired-up frontend, which isn't ready yet.

Once ready, this should cover org-admin-specific actions relevant to
this module: customizing certificate templates, choosing ID card
templates, and reading the Analytics dashboard.