# Frontend UI

Frontend for this module is built by Saeed. This lists what's been
shared with the backend so far, for integration purposes only.

## React screens shared (early version)

- GenerateCertificatePage.tsx
- BulkGenerateCertificatesPage.tsx
- CertificatesListPage.tsx

These used dummy/simulated data, not wired to the real backend yet.
These files were later deleted from the repo by Saeed.

## Static HTML mockups shared (later version)

- generate.html, bulk-generate.html, index.html (certificate list),
  certificate-preview.html, templates.html, customize.html

Key open question: bulk-generate CSV now shows event + date per
recipient row instead of one shared event — not yet confirmed with Saeed.

## Marketing site components (not this module)

Navbar.tsx, Footer.tsx, OceanCanvas.tsx, ScrollReveal.tsx — owned by
Amina Sahar, no backend dependency from this module.